# Public Verification Entry Points

`run_reference_model.sh` runs the portable C reference-model unit tests.

SystemVerilog file-vector and DPI-C regressions require a simulator with
SystemVerilog and DPI-C support. See the verification document for the selected
testbench and vector configuration. Vendor-generated FPGA IP is not required
for the codec-level RTL regression.
