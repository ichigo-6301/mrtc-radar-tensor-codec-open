# Register Map

Control registers provide enable, soft reset, status clear, encoder/decoder enable, and codec configuration. Status registers expose busy state, sticky done/error state, byte and block counters, error codes, and IRQ state.

`CAPABILITY` declares RAW, ZERO_RICE, and DELTA_RICE only. RLE_RICE and PointCloud capability are outside the RDTC v1 public scope.
