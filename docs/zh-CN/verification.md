# 功能验证

验证链覆盖 MATLAB 向量生成、C bit-exact reference model、DPI-C 调用和 SystemVerilog RTL regression。已发布 legal-vector 覆盖 RAW_BYPASS、ZERO_RICE、DELTA_RICE、多 block、AXI packing、loopback、backpressure 和 malformed-stream 负向条件。

公开结果仅适用于记录的向量和配置，不等同于形式穷尽证明或覆盖率 closure。
